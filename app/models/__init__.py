"""ORM models exported for migrations and service code.

Model modules define database persistence shape only. Request validation and
response serialization belong to ``app.schemas``, while business rules belong
to ``app.services``.
"""
