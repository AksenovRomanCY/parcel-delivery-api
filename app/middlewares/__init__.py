"""Custom FastAPI middleware modules.

Middleware in this package runs before route dependencies. The current session
middleware supports the legacy anonymous ownership flow used when JWT auth is
disabled.
"""
